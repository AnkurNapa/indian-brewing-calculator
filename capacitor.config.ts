import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ankurnapa.indianbrewingcalculator',
  appName: 'Indian Brewing Calculator',
  webDir: 'out'
};

export default config;
